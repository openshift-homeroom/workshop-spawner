apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: spawner
spec:
  rules:
  - http:
      paths:
      - path: "/"
        backend:
          serviceName: spawner
          servicePort: 8080
